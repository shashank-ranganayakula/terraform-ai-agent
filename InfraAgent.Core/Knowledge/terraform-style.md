# Terraform project standards

Generate provider, main, variables, outputs, terraform.tfvars, and README files when the request requires a complete Terraform project. Keep user-provided values exact, use valid Terraform HCL, and avoid hardcoded cloud credentials.
